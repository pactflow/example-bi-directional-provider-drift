{
  "name": "product-service",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "lint": "eslint",
    "start": "cross-env NODE_ENV=test node server.js",
    "start:postgres": "cross-env NODE_ENV=test REPOSITORY_TYPE=postgres DB_HOST=localhost DB_PORT=5432 DB_USER=postgres DB_PASSWORD=postgres DB_NAME=product_service node server.js",
    "db:start": "docker compose up -d",
    "db:stop": "docker compose down",
    "db:reset": "docker compose down -v && docker compose up -d",
    "state-server:start": "node ./automation/setup-postgres-state-http.js server",
    "state-server:stop": "lsof -ti:9000 | xargs kill -9 2>/dev/null || true",
    "test": "npm run test:inmemory && npm run test:postgres",
    "test:postgres": "cross-env NODE_ENV=test npm run db:start && npm run state-server:start & npx jest --colors --testTimeout 30000 --testMatch \"**/*-postgres.test.js\" && npm run state-server:stop",
    "test:inmemory": "cross-env NODE_ENV=test npx jest --colors --testTimeout 30000 --testMatch \"**/api-inmemory.test.js\""
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "body-parser": "^2.2.2",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@pact-foundation/pact-cli": "^18.0.0",
    "@pactflow/drift": "^2603.0.5-beta.18",
    "dotenv": "^17.3.1",
    "cross-env": "10.1.0",
    "eslint": "^10.1.0",
    "jest": "^30.3.0",
    "globals": "17.4.0"
  },
  "license": "ISC"
}
