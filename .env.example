# Repository Configuration
# Options: 'inmemory' (default) or 'postgres'
REPOSITORY_TYPE=inmemory

# PostgreSQL Configuration (only used when REPOSITORY_TYPE=postgres)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=product_service
